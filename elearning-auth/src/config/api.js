// API configuration
const API_CONFIG = {
  // JSONPlaceholder is a free online REST API that provides dummy data
  BASE_URL: 'https://jsonplaceholder.typicode.com',
  ENDPOINTS: {
    USERS: '/users',
    MEMBERS: '/members'
  }
};

module.exports = API_CONFIG;